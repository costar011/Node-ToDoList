const HOME = "/";
const WRITE = "/write";
const DETAIL = "/:id/detail";
const DELETE = "/delete";

const routes = {
  home: HOME,
  write: WRITE,
  detail: DETAIL,
  delete: DELETE
};

export default routes;
